package Pensio::Examples;

use strict;

our $installation_url = 'https://testgateway.pensio.com';
our $terminal         = 'MerchantName Test Terminal';
our $username         = 'api@merchantdomain.com';
our $password         = 'xxx';
our $shared_secret    = 'xxx';

sub getRandomOrderId {
	my @chars = ("A".."Z", "a".."z");
	my $string;
	$string .= $chars[rand @chars] for 1..8;
	
	return $string;
}
